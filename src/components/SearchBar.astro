---
interface Props {
  placeholderText?: string;
}
const { placeholderText = "Search..." } = Astro.props;
---

<input
  id="search"
  type="text"
  placeholder={placeholderText}
  class="w-full max-w-md mb-8 px-4 py-2 rounded border"
  autocomplete="off"
/>